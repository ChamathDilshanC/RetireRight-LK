"""
Models placeholder

The application no longer uses a persistent database or SQLAlchemy.
This module remains as a placeholder for compatibility but does not
provide any database-backed models. All user/profile/calculation
data are handled in-memory in the routes (non-persistent).
"""
